interface:
  display_name: "Prove It"
  short_description: "Attach focused runtime proof to a pull request."
  default_prompt: "Use $prove-it to verify this pull request and attach focused runtime proof."
policy:
  allow_implicit_invocation: false
