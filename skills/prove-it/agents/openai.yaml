interface:
  display_name: "Prove It"
  short_description: "Create focused runtime proof for changed code."
  default_prompt: "Use $prove-it to verify the changed behavior. Create local proof for uncommitted or pre-PR work, or publish it when the work is already on a pull request."
policy:
  allow_implicit_invocation: true
